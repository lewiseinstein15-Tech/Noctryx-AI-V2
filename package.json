{
  "name": "noctryx-ai-v2",
  "version": "2.0.1",
  "description": "Jexi AI by Noctryx - sharp-witted personal assistant",
  "type": "module",
  "scripts": {
    "build": "node build.js",
    "sync": "npx cap sync",
    "android": "npx cap open android",
    "build:android": "npm run build && npx cap sync android && cd android && ./gradlew assembleDebug",
    "dev": "node server.js",
    "start": "node server.js"
  },
  "dependencies": {
    "@capacitor/android": "^6.0.0",
    "@capacitor/core": "^6.0.0",
    "@capacitor/splash-screen": "^6.0.0",
    "@capacitor/status-bar": "^6.0.0",
    "express": "^4.19.2",
    "cors": "^2.8.5"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0"
  }
}